# 2026-08-18

## 创建 3 个本地化工作流（skill）+ 2 个配套脚本

按用户要求，把"快速按流程完成任务"的 3 个工作流落成可加载的 skill（项目级 `.workbuddy/skills/`）：

1. **merge-lang-to-mapxml** — 合并其他语言（eng/pol/ru/cz…）到 `Localization/map_xml/*.xml`。
   - 文本型（eng/ger）走 `loc_tools extract-batch` / `append`（按 MD5）。
   - 二进制 cif 型（pol/ru/cz）走新建 `Tools/integrate_lang_xml.py`：按 `map.dat` MD5 匹配，内部用
     `Cultures-map-editor/supplements/initialization.py:decode()` 解 cif，并因游戏按 cp1252 解释字节、
     实际为 cp1250，做 `encode("cp1252").decode("cp1250")` 还原（否则变音乱码）。写入前自动备份 map_xml。
   - 注：原 `integrate_pol_xml.py`（git 319af20/6e9f546）已从工作树删除，本次重建为通用版。

2. **merge-translate-text-history** — 合并/翻译到 `Localization/text`（ger=源、l10=产物），
   补齐 2 代 Cultures 战役 history：l10 history 比 ger 缺 `fonts/` 与 `palettes/` 二进制资源（须从 ger 拷）；
   `history.txt`/`mythology.txt` 按 `[blockstart:KEY]` 与 `.hlt` 引用对齐；CJK 规则：半角'→全角'、überschrift/zurücktext
   等元音变音 key 对齐、段落数严格对齐。

3. **validate-xml-translations** — 以 ger 为基准语义校验 XML 内翻译。新建 `Tools/qa_translations.py`：
   逐 XML 查 缺失键 / 空翻 / 段落数不一致 /（可选 --terms）术语偏离 `language_union.csv`。
   冒烟测试扫出 181 处 ger↔CHN 段落对齐问题（多为 briefing 段数差），证明语义校验必要。
   （`loc_tools validate` 仅结构校验，不够。）

全部文件 LF、未提交（等用户审阅）。下一步：用户审阅后可按需运行 工作流1 实合并波兰语、或跑 qa_translations 修 181 问题。

## 用户三点修正（已落地到工作流 + 脚本）

用户指出：① cif 就是 ini 的序列化格式，`G:/Projects/Cultures_Saga_CN/cif2ini.py` / `cif2ini_batch.py` 可完美解，通常直接给 ini；
② UTF-8 已实现，编码不再是问题；③ 部分地图 MD5 可能对不上，合并时必须极谨慎核对 id 一致性以防错合。

据此修订：
- **integrate_lang_xml.py 重写**：删除 `Cultures-map-editor decode()` + `encode("cp1252").decode("cp1250")` hack；
  改调 `cif2ini.py` 的 `cif2ini_content()` 解 cif（ini 优先），解码统一用 `loc_tools.detect_encoding`；
  合并进 XML 的语言节点 `encoding="UTF-8"`。新增 **MD5 / id 双匹配 + id 一致性闸门**：MD5 优先；
  MD5 对不上时回退"目录名 ↔ XML 的 map_id/export_map_id"匹配；**非 MD5 命中必须做 id 重叠校验**
  （源 string id 须覆盖目标 ≥90%，否则判定疑似错误地图并跳过告警，绝不盲合）。
- **3 个 SKILL.md 同步更新**：去掉 cp1250 说辞，改为 cif2ini + UTF-8；工作流1 强调 MD5 可能不符、
  id 闸门为防错合核心；工作流2 的 ger cif 解码也改走 cif2ini。
- 冒烟测试（内联 python）验证：全覆盖源→按 id 名称匹配整合成功；id 错位源→被 ID 闸门拒绝（no blind merge）。
- 注：用户偏好直接提供 ini，cif 仅作兜底；今后合并脚本默认吃 ini。

## 检查 XML「错误」的正确方式（重要纠错）
- **XML 结构/错误检查用现成工具 `Tools/loc_tools.py validate -i <xml目录>`**，它只校验结构完整性
  （必填字段 version/map_id/languages/strings/briefings 是否齐全 + 能否正常解析），输出 `[OK]`/`[ERROR]`。
- **不要自创"段落对齐/占位符/乱码"之类规则当"错误"报**：用户不认这类维度，且之前检查从未有过这些，
  容易被视为过度设计/噪声。今天全量跑 `validate` 结果：128 个 XML 全部 `[OK]`，0 错误。
- 若确需翻译质量（错位/空翻）检查，应先与用户确认是否要、用哪个维度，再动手，不要默认当成"错误"。

## 优化仓库/记忆/skill（用户指出"有 skill 却没用 + 自建规则当错误"）
- 用户核心不满：项目已有 skill，QA 时我却没用，且自建 qa_translations.py 把段落差异当 ERROR 报，用户不认这类"错误"。
- **修正 `validate-xml-translations/SKILL.md`**：明确"检查错误 = `loc_tools validate`（结构校验）"；翻译质量复核(qa_translations.py)改为**可选、仅用户明确要求时做**，结果标为"质量备注"而非"错误"，且强调用到工作流前先 `Skill` 加载流程。
- **修正 `merge-lang-to-mapxml` / `merge-translate-text-history`**：删除"UTF-8 已实现、编码不再是问题、交给 detect_encoding"的**过时错误结论**（2026-08-18 已实锤 cp1250 陷阱），改回"源文件仍是原始代码页字节，按语言→代码页显式解码"。
- **`Tools/qa_translations.py`**：段落差异从 ERROR 降级为 WARN（仅 缺失/空翻/乱码/占位符 仍为 ERROR）。
- **`.gitignore`**：新增 `Localization/map_xml.bak_*/` 忽略合并备份。
- **`MEMORY.md`**：新增约定"XML 错误检查唯一用 `loc_tools validate`；翻译质量是另一回事，不要自创维度当错误报；用到工作流前先加载 skill"。
- 实测：loc_tools validate 全 128 XML 全 [OK] 0 错误；qa_translations 现仅 7 处真实空翻(eng/pol 个别 briefing 块) + 306 WARN(段落换行差异)。

## 翻译质量审计校准（针对用户"空翻/漏翻/错行频发"的二次整改）
- 用户再批：之前那段"167 ERROR/146 WARN"仍是噪声，且反复不用已建好的 skill、不先看清数据结构就拍脑袋定规则。
- **根因定位**：XML 里语言码是 `"CHN"`(大写)，自写脚本用小写 `"chn"` 查字典 → 全部 CHN 误判缺失（假报 5066）；行数差异对中文是伪信号。每次失败都因**写规则前没先确认真实键名/存储格式**。
- **新工具 `Tools/audit_translations.py`**（替代噪声的 qa_translations.py）：ger 基准、大小写归一化、三类口径——空翻=明确缺陷/漏翻=缺陷/错行=待核对。生成 `translation_audit.md` + `audit.json`。
- **校准后真实数字（156 XML=map_xml128+map_xml_user28，XML 在 Campaign00/01 子目录）**：
  - 空翻 eng 16 + chn 29 = **45 处明确缺陷**（全在 briefings）。
  - 漏翻 key 级 = 0；eng 整文件缺失 = 42 个（多为 `_campaign_04_*` 与 `XX_*.xml` 剧情文件未翻英）。
  - 错行 chn 160 处，抽样核实**均为中文正常换行压缩、译文完整、非截断**（行数启发式对中文是伪信号，勿逐条改）；eng 6 处（campaign_01_07 b:wikiflucht ger=11/eng=2 疑似真截断）。
- 同步重写 `validate-xml-translations/SKILL.md` 指向 audit 工具 + 三类口径 + 大小写坑；`MEMORY.md` 记入口径与现状。
- 结论给用户：先修 45 处空翻，错行(尤其 chn)基本可忽略；42 个缺 eng 文件是否补译由用户定。

## 整理 language_union.csv 为 20 语言母表
- 背景：游戏原生支持 0–19 共 20 种语言（`language_id-zh-cn.md`：ger0/eng1/fra2/ita3/cze4/rus5/pol6/spa7/por8/hun9/l10简中10…l19）。把术语并集表对齐到该体系。
- 改造：列名由 `LANG_ENG/GER/POL/CHN` 改为游戏语言码，按 ID 顺序排成 20 列 `ger,eng,fra,ita,cze,rus,pol,spa,por,hun,l10,l11…l19`；原 ENG/GER/POL/CHN 映射到 eng(1)/ger(0)/pol(6)/l10(10)，其余 16 列留空占位；保留 3 个 META_* 元列。共 23 列、871 行，0 重复行。
- 注意：`LANG_POL` 原即 100% 为空（仅占位）；fra/ita/cze/rus/spa/por/hun/l11–l19 目前也全空，待后续填译。
- 兼容性：活跃 QA 工具 `audit_translations.py` 不读此文件；仅废弃的 `qa_translations.py` 与 `archives/` 下历史脚本引用旧列名，重命名不影响当前流水线（若重跑废弃脚本需相应更新列名）。

## 修复 language_union.csv「串行错行」（关键发现）
- **根因**：用户反映打开后串行错位。检查发现 `language_union.csv` 实际是 **xlsx 压缩包**（文件头 `PK\x03\x04`，含 `xl/worksheets/sheet1.xml` + `sharedStrings.xml`），即被 Excel 另存为 xlsx 却保留了 `.csv` 扩展名。纯文本读写会把二进制压缩包当乱码，导致错行。
- **修复**：用 Python `zipfile`+XML 解析把 sheet 数据完整导出（872 行=表头1+数据871，23 列结构完好，无数据丢失），重写回真正的 UTF-8 文本 CSV（带 BOM，便于 Excel 中文显示）。
- **标注 l10–l19**：按 `language_id-zh-cn.md` 给 l10–l19 表头加括号语言名：`l10(简体中文)/l11(繁体中文)/l12(日语)/l13(韩语)/l14(印地语)/l15(阿拉伯语)/l16(孟加拉语)/l17(印度尼西亚语)/l18(土耳其语)/l19(斯瓦希里语)`。前 10 列 ger/eng/fra… 已是自解释 ISO 码，不动。
- **去毛刺**：xlsx 会丢行尾空单元格导致数据行不足 23 列，已统一补齐为矩形 23 列（`QUOTE_MINIMAL` 自动处理含逗号字段）。
- 教训：该项目 `.csv` 文件可能被 Excel 误存成 xlsx-with-.csv-name；今后改动前先用 magic bytes（`504b0304`=zip）确认是文本还是伪装压缩包，避免把二进制当文本读写。

## 全面接管修复 language_union.csv（用户反馈"全部错了"）
- **根因确认（重要）**：根目录 `language_union.csv` 是**未跟踪新文件**；权威数据源在 **git 已提交的 `archives/language_union.csv`**（7 列 LANG_ENG/GER/POL/CHN+META_*，872 行=表头1+数据871，UTF-8-sig，POL 列 100% 空、ENG 空 307、GER 空 5）。
- **上一个 AI 的核心错误**：20 语言母表重写时**数据列串位**——中文(LANG_CHN)被塞进 fra 列、META_TYPE/DESCRIPTION/REFERENCES 被塞进 ita/cze/rus 列，l10 与 META_* 全空（语言特征检测实锤：fra=CJK 200、ita=CJK 200、rus=ascii 199）。我上一轮 xlsx→csv 转换是忠实的，问题出在源数据已坏。
- **修复**：从 `archives/language_union.csv` 按**列名映射**（不是位置）重建 23 列母表；l10-l19 保留括号语言标注；验证每列语言特征全部正确（ger 含 145 德语变音、l10 全 CJK、META_* 归位、871 行矩形）。
- **顺带数据修复**：eng 列有 24 行是"德语词占位"（原版从德语文本提取术语未对照英文，如 Läufer/Wachtürme）。全库搜 XML 独立字符串后仅 2 行有可验证英文：`Läufer→Runner`(campaign_03_06 string77)、`Hafenstädtchen→Port`(campaign_02_03，l10 港口小镇吻合)。其余 22 行术语只出现在 XML 句子内部，无独立英文词条，**未擅改**，留待用户决定是否人工补译。
- **原版表数据画像**：eng 列=307 空 + 245 专有名词(eng==ger 正常) + 314 真英译(如 Gabelbart→Forkbeard)；ger 空 5 行全为 campaign_04 C2M 词（Erdrachen/Wachdrachen/Grauholm/Wamugo/Kräutersammler）；l10 全填、0 空。
- **注意**：根目录文件仍未提交；若要与仓库同步（更新 archives 或提交根文件），由用户决定。

## 修复 eng 列全部德语词 + 表挪到根目录
- **规模比预想大**：全量扫描发现 eng 列德语普通名词共 **63 处**（不只 22 个带变音符的，还大量不带变音符的如 Sumpfvolk/Lebensenergie/Elfenbein/Schlangenbrut/Hungersnot…，变音符检测漏了大半）+ 5 处 C2M 德语词。
- **修复口径**：
  - 游戏 XML eng 句子有实证的用游戏原文：Wachtürme→defence towers、Langbogenschützen→longbow archers、Kurzschwertkämpfer→shortsword fighters、Schwerter der Könige→Swords of the Kings、Kräuter→herbs、Münzen→coins、Tränke→healing potions、Sümpfe→swamps、Moorvolk→swamp tribe、Läufer→Runner、Hafenstädtchen→Port（共 11 处 [V]）。
  - 无游戏实证的标准英译（52 处 [T]）：vault/famine/ivory/whirlpool/coelacanth/Aesir/Njord/Valley of the Kings…（Fladenscheißer→flatbread pooper、Beduinenschaukler→Bedouin rocker 为诙谐干扰项，建议复核）。
  - C2M（campaign_04 文件仅 chn+ger 无 eng）：Erdrachen/Wachdrachen/Kräutersammler 德语词**挪回 ger 列、eng 留空**；Grauholm/Wamugo 人名 ger=eng=名字。
  - **保留**：Warägergarde→Waräger Guard（游戏 eng 原文即如此，campaign_01_06 实证）；Wyonäzer（专名）；英德同形词（Farm/Gold/Mission/Obelisk/Aura/Fata Morgana…）；全部人名/神名/地名。
- **文件位置**：`git rm --cached archives/language_union.csv` + `git add language_union.csv`，**表已正式挪到根目录、不再归档**（已暂存，未提交）。
- 最终状态：871 行 × 23 列矩形；eng 列仅剩 2 处带变音符（Waräger Guard/Wyonäzer，均为刻意保留）；l10 全中文；META 归位。

## 全仓库整顿（用户要求：整理仓库/记忆/文档/工具/skill + 为 20 语言翻译做准备 + 防 AI 失忆）
- **盘点结论**：README-zh-cn.md、Game_FileSystem_Intro.md、MEMORY.md、3 个 SKILL.md、language_id-zh-cn.md 均新且一致；
  **Tools/README.md 严重过时**（描述的 translate_cli/audit_all_maps/trans_scan/scan_map_dat/build_maps_from_csv/cultures2_converter 等工具全不存在，还引用废弃的 Localization/ZH-CN/ 路径）→ 已按当前实际工具整篇重写。
- **关键缺陷修复**：`.workbuddy/`（skills+memory）原被 gitignore，克隆仓库即丢失 → 改 .gitignore 放行 `.workbuddy/skills/` 与 `.workbuddy/memory/`，随仓库提交（防 AI 失忆根因）。
- **新增** `20-language-translation-guide.md`（根目录）：20 语言 ID↔代码表、代码页规则、接入任意新语言端到端流程（merge→text→术语表→audit→build→收尾）、质量红线、资源路径。
- **更新**：README-zh-cn.md（仓库结构补新文档、支持的语言接 20 语言体系）；MEMORY.md（新增「AI 执行铁律」节 + 20 语言体系节 + 术语表现状）；3 个 SKILL.md（补语言代码全集、总流程/术语表交叉引用）。
- **暂存（未提交）**：.gitignore、README-zh-cn.md、Tools/README.md、20-language-translation-guide.md、language_id-zh-cn.md、language_union.csv（根）、archives/language_union.csv 删除、.workbuddy/ 全部 7 个 md。共 13 项。
- **遗留待用户决策**：① 工作树仍有 ~350 个未提交变更（8-18 波兰语合并 + cif→ini 转换 + 新工具/文档未跟踪）——建议分两批提交（地图合并一批、工具链一批）；② `qa_translations.py` 已废弃但仍在 Tools/，可删；③ `archives/汉化工作指南.md` 为旧归档文档，内容可能过时。

## 提交 + 推送 + 全量 XML 质量检查（用户指令）
- **提交 3 个**：`3c5e50f`(docs 全仓库整顿)、`5180e87`(feat 8-18 本地化收尾：campaign_02/03 POL 合并 74 XML + cif→ini 273+ 删除 + 新工具/审计产物)、`cb7f10d`(chore 移除 OriginalText 冗余 cif)。已推送 `origin/main`（88dc796..cb7f10d），本地=远程。
- **决策**：`MovieOriginal/`（230MB 影片，今天新出现）加入 .gitignore 不入库；工作树发现的 Movie/Ger、Movie/Pol 删除**已 git restore 恢复**（避免已跟踪内容丢失）；`OriginalText` 下 cif→ini 删除补齐提交（ini 均已在库）。
- **全量 XML 质量检查（156 个 = map_xml 128 + map_xml_user 28）**：
  - **结构**：`loc_tools validate` 全部 `[OK]`，0 错误（map_xml_user 需分别校验 Campaign00/01 子目录）。
  - **空翻 45 处**（chn 29 + eng 16，全在 briefings）：多数在 `_campaign_04_*`(C2M) 与 campaign_01_09-sub/02_02/03_0x，清单见 audit.json/translation_audit.md。
  - **漏翻**：key 级 0；eng 整文件缺 42 个（全 C2M 用户战役 01_..22_ + _campaign_04_*）。
  - **错行**：chn 160（其中 15 处被标「截断」，抽样 5 处=3 处完整翻译[中文排版压缩]、2 处浓缩摘要[tutorial_007 b:00、multiplayer_007 begin_00，建议补全]）；eng 6（真截断 1 处=campaign_01_07 b:wikiflucht ger 11/eng 2，优先修）。
  - 审计产物 `translation_audit.md`/`audit.json` 与已提交版完全一致（无新 diff）。

## 修复 XML 质量问题（用户指令）+ 影片目录转移入库
- **影片转移**：用户确认 Movie/Ger(7)+Movie/Pol(2) 已移至 MovieOriginal/（新增 Pol 4 个），提交 `9e5a21c`（git 识别 9 重命名+4 新增，230MB）后台推送中。
- **45 处空翻真相（重要修正）**：逐块核查后发现**多数是"幽灵块"**——`<lang code="ger"/>` 也为空，且游戏源 briefings.txt 里同样是 `[blockstart:X][blockend:X]` 空块（抽查 campaign_02_02 b:07、_campaign_04_02 b:11、campaign_03_06 b:20 等 6 处全空）→ 源无内容，**不可也不应编造**。另有 3 处是**纯图片块**（ger 内容仅坐标如 `1,24,287`）→ 审计误报。
- **真正可修的 8 处已全部修复**（均按 ger 全文翻译）：
  - eng 空翻 3：campaign_01_05 b:sub、campaign_01_07 b:spieler_attakiert_Byzanz、b:spieler_zahlt_tribut
  - eng 截断 1：campaign_01_07 b:wikiflucht（ger 11 行→eng 原仅 2 行，全文重译）
  - chn 浓缩摘要 4：tutorial_007 b:00、multiplayer_007 begin_00、tutorial_001 b:00、multiplayer_02_06 begin_00
- **修复后审计**：eng 空翻 16→13（剩 13 全是幽灵/图片块）、eng 截断 1→0；chn 错行 160→157、截断 15→11（剩余 11 处抽样全部为完整翻译的中文排版压缩，非真截断）。结构校验 128/128 OK。
- **写入技巧**：briefings 空块结构有 3 种——`<lang code="X"/>` 自闭合、`<lang code="X"><text/></lang>`、`<lang code="X"><text>内容</text></lang>`；按块 id 定位后用正则替换，保留原缩进与 CRLF。

## GER 严格一致性审计（XML vs GAME_2/3/4/5_MAP 源，用户指令）
- **工具**：新增 `Tools/ger_source_audit.py`（可复用）——源索引（GAME_2/3/4/5_MAP/GER + GAME_5_MAP_USER/GER/Campaign00|01/currentusermap）+ loc_tools 同款解析（StringsParser/BriefingsParser）+ 逐条比对。报告 `ger_audit_report.md`（根目录）。
- **匹配策略（关键经验）**：① **81 个目录跨版本重名**（GAME_3 与 GAME_5 几乎全套重复）→ 纯名称匹配会错选版本；**必须用 translation_version_choose.csv 的版本约束**；② **GAME_2 multiplayer 变体共享同一 map.dat（MD5 相同）**（militär/ressourcen/cap-the-flag）→ MD5 有歧义，靠名称+版本消歧；③ 索引**不能强求 map.dat**——campaign_01_09/-sub 源只有 text/（无 map.dat、XML map_md5 也为空），但 ger 文本齐全可核对。
- **结果**：156/156 全部匹配到正确源；**152 完全一致**（strings+briefings 逐节点精确）；3 处仅 CRLF↔LF 行尾差异（campaign_02_01 b:300、campaign_02_02-sub b:200、campaign_03_06 b:300，内容一致非错误）；1 处 campaign_01_09-sub.xml 多余空 `header` 块（源无，全语言空，模板残留）。
- **幽灵块结论**：0 处「XML ger 空但源有内容」——XML 里所有空块在源 briefings.txt 同样是空块，GER 无缺失。
- **数据完整性注意**：campaign_01_09/-sub 无 map.dat（GAME_2_MAP 与 SAGA_GAME_HACK 均只有 text/），构建时脚本复制受影响，map.dat 可能在 .lib 归档未解包。

## 接手复核（hy3 在 41c426d 后重新检索记忆/skill/改动）
- **重跑工具得地面真相**：`loc_tools validate` 三目录(156 XML)全 `[OK]` 0 错误；`audit_translations.py` 现 eng 空翻13/整文件缺42/错行6(截断0)、chn 空翻29/错行157(截断11)/整文件缺0。
- **空翻真伪复核（关键）**：自写 `_verify_empty.py` 逐块比对 ger，确认 eng 13 + chn 29 = **42 处空翻全部是幽灵块**（ger 源对应块同样为空），**非真缺陷、不可编造填充**。高级 AI 的"45 处明确缺陷待填"结论被推翻→实为 0 真缺陷。
- **错行真伪复核**：自写 `_verify_trunc.py` 按字符数比对。chn 全部 30–40% 字符比（德文 3× 冗长于中文，完整中译本就如此）→ 全部良性换行/压缩，非截断；eng 6 处 abs(行差)<3 更无截断。结论：行数启发式对 chn 确为伪信号。
- **eng 整文件缺 42 的归属**：核对文件名，14 个 `_campaign_04_*` + 28 个 `NN_*.xml` 用户地图，**全部为用户/C2M 战役**（原版即无英文，主战役地图均含 eng），建议中文本地化不补英文。
- **结论**：eng/ger/chn 三项质量已达「无可修真缺陷」；GER 经 `ger_source_audit` 验证 DONE。下一步可转向其他语言（pol 已合并，可接 cz/ru/fra/ita）。
- **改动**：更新 `MEMORY.md` 翻译质量现状节 + `validate-xml-translations/SKILL.md`（空翻须先按 ger 复核真伪、错行明确为良性），避免下一会话重蹈"修 45 空翻"覆辙。临时脚本 `_verify_empty.py`/`_verify_trunc.py` 已删除。
